const ChatbotIcon = ({ className = "text-black bg-purple-100 rounded-full p-2" }) => {
  return (
    <span className={`material-symbols-rounded text-xl ${className}`}>
      smart_toy
    </span>
  );
};

export default ChatbotIcon;
